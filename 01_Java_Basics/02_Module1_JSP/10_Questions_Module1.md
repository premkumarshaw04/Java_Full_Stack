# Question based on Module 1.

## =>Introduction:

### What is the role of javac.
```
javac is a compiler.it is an application which converts source code to byte code.
```
### what is JRE, JVM.
```
JRE: JRE Stands for Java Runtime Environment.
it is an application which contains liabraries(coolection of pre defined Java Programs).
A liabrary contains JAR files(Compressed Java Resources).
JRE is completely responsible for providing the necessary resources for the execution of a java Progragram.

JVM: It stands for Java Virtual Machine.
It is a translator which help us converting Byte code to machine code.
JVM internally uses JIT(Just In Time) compiler to convert Byte code to machine code.


```
### between JDK, JRE and JVM, which one has physical existance.
```
JDK (Java Development Kit) → Has physical existence
It is a software package installed on your system containing the compiler (javac), JRE, libraries, and development tools.

JRE (Java Runtime Environment) → Has physical existence
It is also installed on your system and contains the JVM and runtime libraries needed to run Java programs.

JVM (Java Virtual Machine) → No separate physical existence
It is a virtual/abstract machine that exists as a process in memory when a Java program runs. 
It is implemented inside the JRE.

In one line:
JDK and JRE physically exist as installed software packages, while JVM is a virtual machine that runs inside the JRE.
```
### explain java program lifecycle.
```
Once we write a java program, it has to be saved in destination as .java Format.
This program will be verified with javac compiler.

If it finds any syntax mistake, then we get compile time error(BYTE CODE IS NOT GENERATD). 
If not javac generates Byte code.

Once the byte code is generated, It will be verified by JVM, If it finds any logical mistake, 
then we get exception(Program execution stops immedietly). 
If not JVM generates the machine code which will be processed by the machine and gives the final output.

```
### what is bytecode.
```
Bytecode is the platform-independent intermediate code generated by the Java compiler 
and stored in .class files, which is executed by the JVM to run a Java program.
```
### explain why java is considered as Platfrom Independent.
```
Java is considered platform independent because its source code is compiled into bytecode (.class file), not directly into machine code of a specific operating system.

The same bytecode can run on any system that has a compatible JVM (Java Virtual Machine) installed. The JVM converts the bytecode into machine code suitable for that particular platform.

For example, a Java program compiled on Windows can run on Linux or macOS without changing the code, as long as a JVM is available on those systems.

That is why Java follows the principle: “Write Once, Run Anywhere (WORA).

Important clarification (people often mix this up):
Java itself isn't platform independent — the JVM is platform dependent (you need the right JVM for the right OS). It's the bytecode that is platform independent, because it behaves the same regardless of where it runs, thanks to the JVM acting as a translator layer underneath.
```
### what are comments in java.
```
Comments in Java are non-executable statements ignored by the compiler, used to improve code readability. Java supports single-line (//), multi-line (/* */), and documentation comments (/** */), the last of which is used by the javadoc tool to auto-generate documentation.
```

## =>Keywords:
### what are keywords.
```
Keywords in Java are reserved words that have a predefined meaning and are used by the Java language to perform specific tasks. They cannot be used as variable names, class names, or method names.
Keyword will help us in buiding java program as well as in providing Program Structure.
```
### Write all 53 keywords according to Java Version 8.(depending on version to version the no of keywords are changing).
```
# Java Keywords (53) — Java 8

abstract, assert, boolean, break, byte, case, catch, char, class, const, continue, default, do, double, else, enum, extends, final, finally, float, for, goto, if, implements, import, instanceof, int, interface, long, native, new, package, private, protected, public, return, short, static, strictfp, super, switch, synchronized, this, throw, throws, transient, try, void, volatile, while, true, false, null.

=> `true`, `false`, and `null` are technically **literals**, not keywords — but are reserved words and cannot be used as identifiers, which is why they're often counted alongside keywords (50 + 3 = 53).
```

### What are Identifiers and Explain the rules for Identifiers.
```
Any names that is given by the programmer is called as an identifier.
Generally the programmer is responsible for providing class names, method names and variable names.

RULES FOR IDENTIFIERS:
1. Identifiers names must always begin with alphabets.
2. Numbers area allowed but not as the first character.
3. Special character "$" and underscore("_") are allowed but not recommended.
4. Keywords can not be used as identifiers.
5. Identifirs can not have blank spaces.

```

## =>Datatypes and Variables

### what are datatypes?
```
Data types in Java define the type of data a variable can store and the operations that can be performed on it.

Java has two categories of data types:

=>Primitive data types – store actual values directly.
byte
short
int
long
float
double
char
boolean

=>Non-primitive (reference) data types – store references to objects.
String
Arrays
Classes
Interfaces
Objects
```
### what are premitives?
```
Primitive data types in Java are the basic built-in data types that store actual values directly, not references.

```
>Java has 8 primitive data types:

| Data Type | Example |
|-----------|---------------------|
| byte | byte a = 10; |
| short | short b = 200; |
| int | int c = 5000; |
| long | long d = 100000L; |
| float | float e = 5.5f; |
| double | double f = 10.25; |
| char | char g = 'A'; |
| boolean | boolean h = true; |

>Primitive types are used for efficient memory usage and faster processing because they store the value directly.
### what are varaibles.
```
Variables are the containers or data holders, which is used to store values.
Every variable must be declared with a data type, which determines the type of data it can store and how much memory will be allocated.
```
### what is Re-initialization? How to prevent re-initialization.
```
Process of reassigning value to a variable is called as reinitialization.
A variable can be re-initialized multiple times.

Re-initialization of a variable can be prevented by declaring it as final. A final variable can be assigned a value only once.

```
### how to create constant values in java.
```
In Java, constant values are created using the final keyword. A variable declared as final can be assigned a value only once, and its value cannot be changed later.

example:
final double PI = 3.14159;
```
### what are the different ways to create a variable in java.
```java
In Java, variables can be created in two ways:

//Declaration only – creating the variable without assigning a value.
int x;
//Declaration with initialization – creating the variable and assigning a value at the same time.
int x = 10;

//Important points:
A variable must be declared before it is used.
Initialization can be done during declaration or later in the program.
If a local variable is declared without initialization, it must be assigned a value before use.
```

## =>Operators

### what are Operators.
```
Operators in Java are special symbols used to perform operations on variables and values.
Operators help in performing calculations, comparisons, logical decisions, and value assignments in a Java program.

Java operators are mainly classified into 7 types:

Arithmetic operators (+, -, *, /, %)
Relational operators (==, !=, >, <, >=, <=)
Logical operators (&&, ||, !)
Assignment operators (=, +=, -=, *=, /=)
Unary operators (++, --, +, -, !)
Bitwise operators (&, |, ^, ~, <<, >>)
Ternary operator (?:)


```
### Difference between "=" and "==".
```
= is the assignment operator, used to assign a value to a variable.
== is the realational Operator checks whether two values are equal and returns a boolean result (true or false).
```
### Difference between post and pre operation?
```
Post : In case of Post opeartion, first the values in the variable will be used and then internally increment and decrement is done. ex: variable++;  and variable--;

Pre: In case of Pre operation, first increment or decrement is done then the value in the variable will be used. ex: ++variable;   , --variable;
```

## =>Control Statements

### After If how many else can be written.
```
Only one else can be written for an if statement.
```
### After If how many else-if can be written
```
We can write N number of else if statements.
```
### can we write else directly.
```
No, else can be written only after an If block.
```
### can both if and else execute at the same type.
```
No. In an if-else statement, only one block executes. If the if condition is true, the if block executes; otherwise, the else block executes. Both cannot execute at the same time.
```
### what is the use of break keyword.
```
The break keyword is used to immediately terminate the current loop or switch statement and transfer control to the statement that follows it.

Important points:
Used with for, while, do-while, and switch.
Stops further execution of the loop or switch.
```
### what is the use of default in Switch.
```
The default case in a switch statement is used to execute a block of code when none of the case values match the given expression.

Important points:
default acts like the else part of a switch statement.
It is optional.
It executes only when no matching case is found.
```
### Difference between For and While.
```

```
### Difference between While and do-while.
```

```
### Write and explain Syntax of for loop.
```

```
### Write and explain Syntax of while loop.
```

```
### Write and explain Syntax of do-while loop.
```

```

## =>Methods

### What are Methods.
```
A method is named block of code that is reusable in nature.
i. A method should always be declared directly in the class body.
ii. A method gets executed only when it is called.
iii. Once a method is created , it can be called n number of times.
iv. we can declare multiple methods in a class based on our requirements.
v. Methods are also known as Funstions.
vi. Methods make the program modular in nature making it easy to read, understand and modify. 
```
### Explain passing values
```
i. Passing value is the concept of sending values from calling method to called method.
ii. We can pass values with the help of arguments.
iii. In arguments we have two types:
    a. Formal Arguments: These are variables that are created in the method declaration.
    b. Actual Arguments: These are values that are provided when method is called.
iv. The programmer can pass multiple values at the same time by declaring multiple arguments.
v. When declaring multiple arguments the formal and actual arguments are separated by using comma.
vi. It is programmer's responsibility to pass arguments of the correct type, length and sequence.

```
### Explain Returning values
```
Returning value is the concept of sending value from called method back to calling method.
-> to return a value, the programmer has to follow three steps:
i. Declare return type based on value to be returned.
ii. Return a compatible value using return keyword.
iii. Capture the returned value inside calling method.

Note:
If the calling method fails to capture the returned value, the returned value will be considered as 
considered as lost.

If the return type of method is void, it indicates that the current method does not return any value
to the calling method. we can write return but we can not give any value. 
```
### what is Recursion in Methods.
```java
//Recursion is a process in which a method calls itself repeatedly until a terminating condition (base case) is reached.

//A method or methods are said to be in recursion in the following Scenarios:

//Scenario 1:
//In scenario 1, the called method makes a call back to its own calling method.

class Demo{
    static void test(){
        System.out.println("test()......");
        disp();
    }
    static void disp(){
        System.out.println("disp()......");
        test();
    }
}


//Scenario 2:
//In scenario 2, the currently executing method call to itself

class Sample{
    Static void play(){
        System.out.println("play().....");
        play();
    }
}

If recursion is not controlled it will lead to Stack overflow error.
```

## =>Scanner
### What is Scanner? Explain How to use Scanner.
```
It is a inbuilt program of Java liabrary.

The programmer can make use of Scanner to read User input, during program execution.

to make use of Scanner the programmer has to follow three steps:
i. import Scanner from 'java.util' package.
ii. Create an object of Scanner.
iii. Read Values by calling the predefined methods of Scanner.
```

## =>Arrays
### What are Arrays.
```
An array is a finite set of homogeneous values, meaning it stores a fixed number of elements, contains a group of values, and all the elements in the array are of the same data type.

In java the programmer can create arrays in two ways:
i. Using Dimmensions
ii. Using array initializers

i. Using Dimmensions: In this Approach array is created by providing the datatype and size of the array.
Syntax: datatype[] variableName = new datatype[int size]
example: int[] arr1 = new int[6];

ii. Using array initializers: 
In this approach array is created by providing the datatype and actual values.
Syntax: datatype[] variableName = {val1, val2, val3......valN};
ex: double cgpa = {7.5, 8.5, 8.9, 7.9, 8.3, 8.5};

```
### when do we get ArrayIndexOutOfBoundsExeception.
```
If we try to access an invalid index in the array, it will lead to a Runtime failure(Exception) known as ArrayIndexOutOfBoundsExeception.
```

